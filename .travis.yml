language: node_js
node_js:
  - "10"
rvm: 2.4.1

install:
  - npm install -g yarn
  - gem install bundler

before_script:
  - yarn
  - cd website && bundle install

script:
  - yarn build
  - ls
  - cd website && bundle exec jekyll build